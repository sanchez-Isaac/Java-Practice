package java_basics;

//An interface is a "Contract" it becomes, obligatory for each of
// the classes that implement that methods in the interface!!!

public interface Flyable {
    // An interface only has abstract methods, so fly() is abstract or an abstraction
    public void fly();

}
